---
interface Props {
  variant?: "inline" | "hero";
  class?: string;
}

const { variant = "inline", class: className = "" } = Astro.props;

const dimensions =
  variant === "hero"
    ? { width: 200, height: 280 }
    : { width: 88, height: 120 };
---

<figure
  class:list={[
    "pointer-events-none select-none",
    variant === "hero" ? "mx-auto" : "hidden sm:block",
    className,
  ]}
  aria-hidden="true"
>
  <img
    src="/author-mascot.webp"
    alt=""
    width={dimensions.width}
    height={dimensions.height}
    class:list={[
      "h-auto w-auto object-contain opacity-[0.42] dark:opacity-[0.32]",
      variant === "hero" ? "max-h-[280px]" : "max-h-[120px]",
    ]}
    loading="lazy"
    decoding="async"
  />
</figure>
